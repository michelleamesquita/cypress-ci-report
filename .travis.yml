language: node_js
node_js:
  - 11.6.0
cache: npm

install:
  - npm install

script:
  - docker build -t cypress-test .
  - docker run cypress-test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master 
